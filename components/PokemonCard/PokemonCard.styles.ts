import { CSSObject } from "../../types";

const styles: CSSObject = {
    image: {
        width: '80%',
        height: '10rem',
        margin: 'auto',
        objectFit: 'contain'
    }
};

export default styles;
