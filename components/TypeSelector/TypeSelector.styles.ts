import { CSSObject } from "../../types";

const styles: CSSObject = {
  select: {
    borderRadius: 3,
  },
  clearIcon: {
    position: "absolute",
    zIndex: 1,
    right: "1.6rem",
    top: "50%",
    transform: "translateY(-50%)",
  },
};

export default styles;
